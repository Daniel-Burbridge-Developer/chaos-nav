CREATE VIRTUAL TABLE IF NOT EXISTS stops_fts
USING fts5(
  name,
  number,
  content='stops',
  content_rowid='rowid'
);

INSERT INTO stops_fts(stops_fts) VALUES('rebuild');

(make sure to search with MATCH and a wildcard at the end)
